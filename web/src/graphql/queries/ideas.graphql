query Ideas($limit: Int!, $cursor: String) {
  ideas(limit: $limit, cursor: $cursor) {
    ideas {
      id
      title
      description
      cost
      createdAt
      user {
        username
      }
    }
    hasMore
  }
}
